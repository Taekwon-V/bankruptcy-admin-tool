# AI Agent Guidelines (Andrej Karpathy Principles)

이 프로젝트에서 AI 에이전트는 안드레 카파시(Andrej Karpathy)의 4대 에이전트 행동 원칙과 파산관제 업무의 도메인 특성을 항상 준수합니다.

---

## 🏛️ 4대 핵심 행동 원칙

### 1. 생각 먼저 (Think Before Coding)
* 모호한 요구사항이나 여러 해석이 가능한 작업은 임의로 넘겨짚어 코드를 작성하지 않는다.
* 코드를 작성하기 전에 전제와 접근 방식을 명확히 밝히고, 불확실한 부분은 사용자에게 확인을 받는다.

### 2. 단순성 최우선 (Simplicity First - YAGNI)
* 요청하지 않은 불필요한 기능, 복잡한 추상화 레이어, 과도한 엔지니어링을 피한다.
* 주어진 문제를 가장 직관적이고 군더더기 없이 해결하는 최소한의 코드를 작성한다.

### 3. 외과의사 같은 정밀 수정 (Surgical Changes)
* 작업 대상과 직접적인 관련이 없는 주변 코드, 주석, 포맷팅, 파일 구조를 임의로 리팩토링하지 않는다.
* 변경이 필요한 지점만 메스로 도려내듯 정밀하게 수정하여 의도치 않은 사이드 이펙트를 방지한다.

### 4. 목표 기반 실행 및 자가 검증 (Goal-Driven Execution)
* 코드를 작성한 후 방치하지 않고, 명확한 완료 기준(Done criteria)을 수립한다.
* 실행 스크립트, 단위 테스트, 린터, 런타임 결과 확인 등을 통해 에이전트 스스로 검증을 마친 후 완료를 선언한다.

---

## 🔒 파산관제 도구 특화 보안 & 데이터 원칙

1. **100% 오프라인 & 로컬 프라이버시 (Zero External Leak)**
   * 주민등록번호, 계좌 거래내역, 금융 정보 등 채무자/채권자 민감 개인정보는 절대 외부 서버나 클라우드로 전송하지 않는다.
   * 모든 데이터 분석, 엑셀 파싱, DB 저장은 PC 내부(로컬 SQLite / 로컬 파일)에서만 처리한다.

2. **국내 문서 규격 및 폰트 안전성 (Korean Encoding & EastAsia Fonts)**
   * DOCX/HWPX/PDF 생성 시 한글 폰트(동아시아 폰트 `w:eastAsia`) 및 인코딩(UTF-8)을 철저히 검증하여 글자 깨짐(Tofu 현상)을 방지한다.
   * 법원 제출 표준 서식(한글 HWPX, 엑셀 배당표 등)과의 완벽한 호환성을 유지한다.

3. **반 화면(Half-Screen / 700~960px) 최적화 UI/UX**
   * 법원 전자소송 화면이나 한글/PDF 서류와 함께 띄워두는 분할 화면(Split Screen) 작업을 기본 전제로 설계한다.
   * 가로 700~960px 너비에서도 가로 스크롤 없이 모든 기능(드래그 앤 드롭, 필터링, 원클릭 복사)이 매끄럽게 동작하는 반응형 세로 레이아웃을 유지한다.
